const AgainModal = ({onCancel}) => {
  return (
      <div
          className='top-0 left-0 w-full h-screen bg-[#0009] fixed'
          onClick={() => onCancel()}>
          salom
          </div>
  );
}

export default AgainModal
