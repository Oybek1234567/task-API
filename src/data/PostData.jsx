// import Posts from "../components/Posts";

const PostData = () => {
  return (
      <div>
          {/* {posts
              ? "User tanlang..."
              : posts.map((item) => (
                    <Posts key={item.id} title={item.title} id={item.id} />
                ))} */}
      </div>
  );
}

export default PostData
